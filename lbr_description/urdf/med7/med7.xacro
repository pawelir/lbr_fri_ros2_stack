<?xml version="1.0"?>

<!-- top level -->
<robot name="med7" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <!-- include the lbr med macro -->
    <xacro:include filename="$(find lbr_description)/urdf/med7/med7_description.xacro" />

    <xacro:arg name="robot_name" default="lbr" />
    <xacro:arg name="mode" default="true" />
    <xacro:arg
        name="system_config_path"
        default="$(find lbr_description)/ros2_control/lbr_system_config.yaml" />
    <xacro:arg
        name="initial_joint_positions_path"
        default="$(find lbr_description)/ros2_control/initial_joint_positions.yaml" />
    <xacro:arg
        name="controllers_config_path"
        default="$(find lbr_description)/ros2_control/lbr_controllers.yaml" />

    <!-- KDL requires a link without inertia / Gazebo requires a connection to world link or robot
    will tipple https://github.com/lbr-stack/lbr_fri_ros2_stack/issues/230 -->
    <link name="world" />

    <joint name="joint_name" type="fixed">
        <origin xyz="0 0 0.3" />
        <parent link="world" />
        <child link="mounting_base" />
    </joint>

    <link name="mounting_base">
        <visual>
            <geometry>
                <box size="0.5 0.5 0.6" />
            </geometry>
        </visual>
        <collision>
            <geometry>
                <box size="0.5 0.5 0.6" />
            </geometry>
        </collision>

    </link>

    <!--joint
        between <robot_name>_world_link and robot_name_link_0-->
    <joint name="$(arg robot_name)_world_joint" type="fixed">
        <origin xyz="0 0 0.3" />
        <parent link="mounting_base" />
        <child link="$(arg robot_name)_link_0" />
    </joint>

    <!-- med -->
    <xacro:med7
        robot_name="$(arg robot_name)"
        mode="$(arg mode)"
        system_config_path="$(arg system_config_path)"
        initial_joint_positions_path="$(arg initial_joint_positions_path)"
        controllers_config_path="$(arg controllers_config_path)" />
</robot>
